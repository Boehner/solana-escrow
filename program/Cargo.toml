[package]
name = "solana-escrow"
version = "0.1.0"
edition = "2021"
description = "A trustless escrow program for Solana — Web2 backend pattern rebuilt as on-chain state machine"
license = "MIT"

[dependencies]
solana-program = "2.2"
borsh = "0.10"

[lib]
crate-type = ["cdylib", "lib"]
